# ClipperTV environment configuration

# Storage backend (gcs or supabase)
CLIPPERTV_STORAGE=supabase

# Supabase configuration
SUPABASE_URL=https://your-project-url.supabase.co
SUPABASE_API_KEY=your-supabase-api-key

# Google Cloud Storage (if using GCS)
# These are typically configured in Streamlit's secrets.toml instead
# GCS_BUCKET=clippertv_data